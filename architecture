pip install 
langchain 
langchain-core 
langchain-community 
langchain-openai 
python-dotenv




APIs like:

OpenAI
Groq
Gemini
Ollama

example 
pdf chat bot 
PDF/Text
   ↓
Loader
   ↓
Text Splitter
   ↓
Embeddings
   ↓
Vector Store
   ↓
Retriever
   ↓
LLM
   ↓
Answer



big picture acrhitecture
                ┌─────────────────┐
                │     User        │
                └────────┬────────┘
                         │
                         ▼
                ┌─────────────────┐
                │ Prompt Template │
                └────────┬────────┘
                         │
                         ▼
                ┌─────────────────┐
                │      LLM        │
                └────────┬────────┘
                         │
          ┌──────────────┴──────────────┐
          ▼                             ▼
   ┌─────────────┐              ┌─────────────┐
   │  Retriever  │              │    Tools    │
   └──────┬──────┘              └──────┬──────┘
          │                             │
          ▼                             ▼
   ┌─────────────┐              ┌─────────────┐
   │ Vector DB   │              │ APIs/Search │
   └─────────────┘              └─────────────┘




for environment loading 


import os 
from dotenv import load_dotenv
load_dotenv()



Why LCEL Matters

Benefits:

composable
async support
streaming
production-ready
clean syntax


Common Loaders
Loader	        Purpose
PyPDFLoader 	  PDFs
WebBaseLoader	  Websites
CSVLoader	     CSV files
TextLoader	     Text files

while training new models we can expect some model conflicts, because with older versions we got some extra library for example langchain.chain but 
while using it now we can say that it can create some errors like with langchain.huggingface 











